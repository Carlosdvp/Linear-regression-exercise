{
  "name": "linear-regression",
  "version": "1.0.0",
  "description": "tensorFlowJS",
  "main": "app.js",
  "scripts": {
    "start": "browser-sync start --server --files '**/*.css, **/*.html, **/*.js' --directory --port 3334 --browser"
  },
  "author": "CDV",
  "license": "ISC",
  "devDependencies": {
    "browser-sync": "^2.24.5"
  }
}
